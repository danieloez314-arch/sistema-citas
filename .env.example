# Configuración del Servidor
SERVER_PORT=8080

# Configuración de Base de Datos
DB_URL=jdbc:mysql://localhost:3306/Barberia_Neita?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=America/Bogota&allowPublicKeyRetrieval=true
DB_USERNAME=root
DB_PASSWORD=root

# Configuración de JPA/Hibernate
DDL_AUTO=update
SHOW_SQL=true

# Notas:
# - Copie este archivo a .env y ajuste los valores según su entorno
# - Para producción, use DDL_AUTO=validate y SHOW_SQL=false
# - Nunca suba el archivo .env al repositorio (ya está en .gitignore)
